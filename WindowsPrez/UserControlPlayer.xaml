<UserControl x:Class="WindowsPrez.UserControlPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WindowsPrez"
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="50">
    <Border
        BorderBrush="Black" BorderThickness="0.5">
        <Grid 
            Background="Purple">
            <Grid.RowDefinitions>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Image 
                Source="/Player.jpg"
                Grid.Row="0"
                Stretch="Uniform"
                x:Name="playerimg"
            />
            <Label
            x:Name="lblNumber"
            HorizontalAlignment="Center"
            Grid.Row="2"
            Margin="0"
            Padding="0"
            Foreground="Yellow"
            FontSize="13"
>
                PlayerX
            </Label>
        </Grid>
    </Border>
</UserControl>
